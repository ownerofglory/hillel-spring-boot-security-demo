spring:
  datasource:
    url: jdbc:h2:mem:hilleldb
    driver-class-name: org.h2.Driver
    username: hillel
    password: hillel123
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
  h2:
    console:
      enabled: true
